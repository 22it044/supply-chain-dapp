{
  "name": "supply-chain-dapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild public/script.js --bundle --outfile=public/bundle.js --format=esm --platform=browser --sourcemap"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^11.6.1",
    "firebaseui": "^6.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
} 